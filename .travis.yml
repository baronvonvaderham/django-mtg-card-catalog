language: python
python:
  - "3.5"

install:
  - pip install -r requirements.txt
  - pip install coveralls

script: coverage run card_catalog/manage.py test
notifications:
  email: false
after_success: coveralls
